let number = Number(prompt("Enter number: "));

switch (true) {
    case (number > 0):
        alert(1);
        break;
    case (number < 0):
        alert(-1);
        break;
    default:
        alert(0);
}
